# MagneticTB 帮助文字逐页审校表

## 2026-09-07 七篇教程文字统一

对照《MagneticTB 中文手册》前十页，将七篇正式教程的 85 段正文改为“先介绍模型，再给输入，接着解释结果”的写法。英文与简体中文同步；章节、代码、参数、矩阵和图形均保留。本次不修改 PDF，也不新增功能、门禁或数学测试。

| 教程 | 文字修改及逐页复查 |
|---|---|
| GettingStarted | 用石墨烯串起五项输入、原子坐标与磁化方向、onsite、近邻跃迁和能带；删去缓存叙述。安装后明确要求完全退出并重新打开 Mathematica。保留多个轨道、多个 Wyckoff 点与含自旋模型。 |
| GeneralExamples | 各模型先说保留哪些轨道、求什么 Hamiltonian，再解释图和矩阵；去掉迁移报告、验证声明、DynamicModule 保存机制。保留 MoS2 等例的参数换写原因及非材料拟合说明。 |
| CrystalAndKPaths | 以 Kagome 的三个磁矩讲结构图、俯视图、倒空间路径和能带，解释 120°、平带与交叉位置；保留坐标约定和 standardKPath 的算法范围。 |
| BandFitting | 按读取三条 VASP 能带、最近邻拟合、第二近邻拟合、能量范围和 K 点附近拟合讲解。说明各条曲线含义，保留“误差小不等于带隙和交叉正确”的物理提醒。 |
| ContinuousSymmetry | 先介绍 124.360 反铁磁模型，再比较有 SOC 和无 SOC 加 C_infinity 时的矩阵元与 G-Z 能带；解释四重简并只出现在指定高对称点，保留连续操作的适用范围。 |
| Corepresentations | 围绕 164.87 的旋量基与标量 s 轨道讲单值、双值结果；明确表格各列和各 k 点标签。依赖手动加载、有序 MSGCorep 操作和直接打印结果的说明均保留。 |
| LegacyBackend | 统一称“使用 MagneticTB 1.0 版”，按加载、石墨烯输入及两版比较讲解；不用“后端、隔离模型”等内部表述。保留两个版本分别初始化、使用不同内核的要求。 |

只生成这七篇的中英文 Notebook，共 14 个文件。已完成字符转义、语法读取和文字片段核对；与修改前逐页比较，171 个 Input/Output/Print 单元在每种语言中均保持不变，中英文相同。仅复用已有计算输出，没有执行示例、全量帮助生成、PDF 渲染或 Paclet 构建。

## 2026-09-07 英文 PDF 手册

按当前中文版完整翻译 `Documentation/MagneticTB manual.tex`，保留相同章节、38 段代码、公式和插图；帮助主页截图保留负责人原图，英文图注说明为简体中文界面。安装、更新、卸载、SOC、诱导表示和非共线 Kagome 的说明与中文版一致，没有新增补充章节。

使用 XeLaTeX 生成同名 PDF，共 17 页。已查看全部页面及最后调整的换行和分页；编译日志无缺图、未定义引用或溢出版心警告。复用中文版模型图，不重新计算模型，不运行帮助重放或发布门禁；未修改 Notebook、程序、测试或中文版。

## 2026-09-07 中文 PDF 手册

本次只改 `Documentation/MagneticTB中文手册.tex` 及其插图和 PDF；未改 Notebook、程序或测试。按负责人要求沿用手册前十页的宋体、章节和代码框，将新功能融入“使用方法”和“例子”，不再保留独立补充说明。

| 内容位置 | 修改及人工复查 |
|---|---|
| 安装、更新和卸载 | 一条命令安装；说明完整重启 Mathematica；加入帮助主页命令及负责人提供的真实截图。 |
| 使用方法 | 按群操作、五项模型输入、近邻、Hamiltonian、画图、接口讲解；去掉内部缓存说明和失效默认值。 |
| 石墨烯 | 显示两带矩阵、结构、BZ、能带；四带 SOC 例明确区分 r1/r2 的含义，用开关 SOC 的两张图解释带隙。 |
| 立方晶格诱导表示 | 从 221 号灰群的参考 px 构造三条基函数，明确实际顺序为 px/pz/py；展示结构、Hamiltonian 和能带，并说明它不是九带完整 p 基的同一模型。 |
| 非共线 Kagome | 复用当前 CrystalAndKPaths Notebook 的四个实际图形输出，不重算未改变的模型；解释 120 度磁矩、单个局域旋量、三带矩阵和平带。 |
| 常见问题和引用 | 保留直接问答语气，删掉不支持自旋群等过期结论；保留 QQ 群号及 MagneticTB 论文引用。 |

插图生成脚本为 `GenerateChineseManualFigures.wls`，可按图组单独运行；没有引入发布门禁。这里只编译和查看这一本手册，未运行全量帮助重放或 Paclet 构建。

最终 PDF 为 16 页。XeLaTeX 编译完成，日志无缺图、未定义引用或溢出版心警告；已查看全书缩略图，并复查调整后的 Kagome 页。短代码块保持整段，不再把右括号单独放到下一页。原手册的宋体、标题、页边距和浅黄色代码框保留。11 张模型插图来自正式 MagneticTB 绘图函数，帮助主页截图按负责人原图收录。

本表以原版《MagneticTB 使用手册》及 `MagneticTB manual.pdf` 为语言基准。审校不只检查代码能否运行，还逐页检查四件事：

1. 是否先说明这一页要解决的物理问题；
2. 是否把输入的物理含义说清楚；
3. 是否解释实际输出，而不是只写程序内部过程；
4. 是否避免测试报告和内部实现的口吻。

“保留”表示原文已经接近原手册的叙述方式，并不表示该页没有经过检查。“改写”表示需要从生成源修改正文。最终生成 notebook 后还要补上“复查结果”。

| 页面 | 首轮判断 | 主要问题或保留理由 | 修改方向 | 复查结果 |
|---|---|---|---|---|
| `Guides/MagneticTB` | 改写 | 开头尚可，但旧版说明像后端说明；缺少新公共精确线性代数入口 | 先说软件能算什么；把 Old 写成“原版函数”；增加分圆域精确线性代数索引 | 通过：主页按入门、群论、Hamiltonian、绘图、物性和分圆域分成六个可见小节；Old 已统一称为原版函数，新增绘图说明已完成中文化 |
| `banddata` | 轻改 | 结果说明清楚；“共享 notebook”略像开发建议 | 改成用户保存和交换数据时的自然说法 | 通过：直接说明每行动量和每列能带，实例显示真实数值表 |
| `bandManipulateold` | 轻改 | 基本顺序清楚，“原实现”重复 | 用“原版”统一称呼 | 通过：先用原版函数完整初始化和求 H，再打开能带滑块 |
| `bandManipulate` | 保留 | 已按路径、参数、图形解释，接近原手册 | 仅润色参数全零时的说明 | 通过：说明动量路径、参数滑块、零初值平带与不作能量平移 |
| `bandplot` | 保留 | 输入、参数和图形作用明确 | 保持简短 | 通过：固定参数的实际能带图与 `plotRange` 的作用分开解释 |
| `brokenSymmetryInitRules` | 改写 | “返回规则、安装新模型”偏程序口吻 | 先说明保留子群会怎样拆分 Wyckoff 轨道 | 通过：先解释子群为何拆分 Wyckoff 轨道，再说明检查新输入和显式初始化 |
| `compareBand` | 保留 | 从 DFT/实验参考能带出发，物理目的清楚 | 解释路径必须一致的原因 | 通过：以 DFT/实验能带对比为目的，明确路径顺序和取样必须一致 |
| `CompileMagneticTBInput` | 改写 | “高级接口、字符串键、检查后的数据”过于工程化 | 明确这是给外部程序准备模型数据的函数 | 通过：从外部程序给出四类物理输入讲起，实例显示真实格点、表示和键 |
| `CurrentModelSession` | 改写 | 全页以内部数据和会话为主 | 明确普通用户何时需要它；内部键只放在后面 | 通过：先推荐三个具体查看函数，再说明何时一次取得完整模型信息 |
| `GenerateGroup` | 轻改 | 定义准确但抽象 | 先用 C4 说明“从生成元补出全群” | 通过：用 C4 四元闭合与平凡群 {E} 起句 |
| `getGenerator` | 轻改 | 兼容说明在物理用途之前 | 先说从完整群找较小生成元集合 | 通过：先说完整群与生成子集，兼容说明已后置 |
| `hop` | 改写 | 多次出现“缓存、匹配选项、记录” | 先分清“symham 结果导出”和“手写 H(k) 转换”两条用法 | 通过：清楚分成导出 `symham` 实空间 hopping 与从手写 H(k) 提取相位两种用法 |
| `initfromrep` | 改写 | 必要技术条件多，连续堆叠成接口规范 | 先说明适用场景，再分条解释群顺序和矩阵顺序 | 通过：先说已知表示矩阵的场景，再逐项解释群元、矩阵和轨道标签顺序；已删除与普通建模无关的额外乘法报告说明 |
| `initold` | 轻改 | 隔离原则正确，但“后端”过多 | 统一写“原版程序/原版函数” | 通过：统一称为原版，完整输入与 onsite Hamiltonian 在同一例中 |
| `init` | 改写 | 选项完整，但部分文字像数据结构说明 | 回到原手册“五类物理输入”的顺序；再说明两种表示模式 | 通过：主要输入按物理顺序说明，两种表示模式后置；当前九项公开选项均有实例，已移除程序不再接受的 `SiteLocalData` |
| `mlgop` | 保留 | 数据库用途和输出格式清楚 | 保持简短 | 通过：操作标签、点矩阵、平移和反幺正标志均有说明 |
| `mrgop` | 保留 | 数据库用途和输出格式清楚 | 保持简短 | 通过：操作标签、点矩阵、平移和反幺正标志均有说明 |
| `msgop` | 轻改 | 格式解释准确 | 增加“这些结果可直接交给 init”的承接句 | 通过：解释每条操作的四部分，并说明结果可直接交给 `init` |
| `orbitalTable` | 保留 | 已逐列说明实际表格 | 统一“轨道编号”说法 | 通过：逐列说明格点、坐标、轨道和自旋，轨道编号对应 Hamiltonian 行列 |
| `pointMatrix` | 轻改 | 数学定义清楚但起句偏抽象 | 先说“想看基函数如何变换时” | 通过：用 C2z 作用于 `{px,py}` 的具体矩阵解释列向量约定 |
| `readHR` | 轻改 | 以格式检查起句，物理内容在后 | 先说每个 R 对应的 hopping 矩阵，再讲文件检查 | 通过：先说 H(R) 及轨道顺序，再说文件完整性检查 |
| `showbonds` | 保留 | 键层、几何和边界说明具体 | 保持 | 通过：键层编号、起点、平移后终点和位移向量均有实际表格 |
| `showHamiltonianBasis` | 保留 | 清楚解释 H 的行列对应关系 | 保持 | 通过：明确 `H[[i,j]]` 的第 `j` 列为初态、第 `i` 行为末态 |
| `showHoppingParameters` | 改写 | “代表性、传播、缓存”较抽象 | 用“这个参数最初是哪条键、还出现在哪些等价键上”解释 | 通过：先列参数对应的初态、末态和平移，再列所有对称等价键 |
| `showMSGWyckoff` | 保留 | 输出各列的物理含义明确 | 保持 | 通过：代表位置、自由参数、允许磁矩方向和 multiplicity 均有说明 |
| `showSymmetryRepresentations` | 改写 | “准备好的表示、compact generators”像内部状态 | 先说矩阵作用于哪组轨道；再解释反幺正信息在操作中 | 通过：明确矩阵沿 orbitalTable 顺序作用，反幺正性只由操作数据携带 |
| `symhamII` | 轻改 | 物理约定清楚 | 用更直接的“移动相位而不改能谱” | 通过：直接说明把格内轨道中心相位移入基底，矩阵变而能谱不变 |
| `symhamold` | 轻改 | 主要受兼容说明支配 | 先说原版键层如何求 Hamiltonian | 通过：先完整 `initold`，再逐键层给出原版 Bloch Hamiltonian |
| `symham` | 改写 | 数学内容正确；“求解、检查等级、工作 Hamiltonian”过硬 | 先从某一键层的 hopping 和 Bloch 相位讲起，算法选项后置 | 通过：先讲实空间 hopping、对称等价元和 Bloch 相位，算法选项后置 |
| `symmetrizationHRInit` | 改写 | “兼容接口、返回 Association”盖过物理用途 | 先说明为 Wannier 轨道准备中心和对称矩阵 | 通过：先说 HR 对称化所需的中心、轨道顺序和矩阵，再逐键解释结果 |
| `unsymham` | 保留 | 清楚说明取消对称约束后的物理意义 | 保持 | 通过：将独立复跃迁与同键层 `symham` 的对称约化结果并排说明 |
| `CompileCyclotomicMatrices` | 新增 | 需要区分结构化 root_sum 输入与普通精确矩阵入口 | 说明统一分圆域、矩阵对象和次数上限 | 通过：输入 schema、共享域、还原流程、维数与次数失败均有实际输出 |
| `CyclotomicRREF` | 新增 | 需要固定内部精确矩阵输入和跨语言索引约定 | 明确 0 基主元/自由列及禁止近似 fallback | 通过：有理矩阵与含 I 矩阵均实际重放，结果含 RREF、秩与零度 |
| `CyclotomicNullSpace` | 新增 | 行基与列基容易混淆，满列秩还需保留二维形状 | 并列解释 `nullspace_rows` 与 `basis_matrix` | 通过：行基/列基、0 基索引、精确残差和 2×0 基矩阵均明确展示 |
| `CyclotomicCommonKernel` | 新增 | 需要说明迭代公共核、空约束与零维边界 | 展示每轮零度并禁止堆叠约束 fallback | 通过：普通公共核、空约束和 0×0 基矩阵示例均通过重放 |
| `CyclotomicCommonNullSpace` | 新增 | 这是普通精确矩阵主入口，必须覆盖两个公开选项与输入范围 | 以普通矩阵主流程贯穿，并列出精确输入/失败边界 | 通过：`CoordinateDimension`、`MaxCyclotomicDegree`、空约束、零维、机器数和不支持代数数均有真实结果 |
| `RestoreCyclotomicExpression` | 新增 | 需要区分可还原对象、RREF 元数据和 context/element 形式 | 展示矩阵、完整核结果与单元素还原 | 通过：行优先 entries、行/列基以及精确 I 均由正式入口还原 |
| `ContinuousSymmetry` | 改写 | 理论正确，但开头连续堆叠“生成元、约束、有限部分” | 先用两态 SO(2) 例子说明哪些矩阵元被禁止，再给一般公式 | 通过：先用两态 SO(2) 权选择定则展示非对角元消失，再讲一般生成元约束 |
| `GeneralExamples` | 改写 | 多处像迁移报告和测试声明 | 按原手册例子写法，逐模型说明“要算什么、怎样输入、结果说明什么” | 通过：逐模型先说物理目标，再给完整输入和 H、能带、等能线、Chern 或 HR 结果 |
| `GettingStarted` | 重点改写 | 内容最全，但工程词和边界说明过多，主线不够像原手册 | 以石墨烯贯穿：输入—检查原子—分层求 H—画能带—看对称性；错误放最后 | 通过：主线已按原手册顺序重排；输入、轨道表、键层、实际 H、能带与对称矩阵均有输出解释；已删除额外乘法失败的旁路示例；11 页真实渲染通过 |
| `LegacyBackend` | 改写 | “后端、context、隔离原则”像开发文档 | 改为“怎样继续运行原版 notebook” | 通过：页面标题和步骤均改为“原版 MagneticTB”，不再使用“后端” |
| `ValidatedModels` | 重点改写 | 标题虽已改，但若干段落仍像测试模型清单 | 改成“典型模型”，每节突出具体轨道、矩形块、表示和能带 | 通过：逐节突出轨道、矩形块、自旋或反幺正作用及实际能带 |
| `WyckoffCrystalSystems` | 重点改写 | 十例文字重复，结尾几乎是验收报告 | 每个晶系说清楚数据库位置和反幺正生成伙伴点；严谨检查移到末尾的简短说明 | 通过：十例均先说数据库种子和反幺正伙伴点，再分别给 DirectProduct/Induced 实际 H、表示和能带；46 页真实渲染通过 |

## 2026-08-15 testing 公开 API 文档增量

| 页面 | 变更 | 审校重点 | 复查结果 |
|---|---|---|---|
| `init` / `initfromrep` / `CompileMagneticTBInput` | 默认键壳数改为 20 | 默认值、显式覆盖和失败不回退 | 中英文同步，显式 `InitialBondShells` 例子保留 |
| `bandplot` / `showband` | 静态能带显示 | 每个绘图选项单独实例，y ticks 与字体均覆盖 | 已由当前 testing API 产生输出；`showband` 只记录已验证自动路径形式 |
| `standardKPath` | 新增 | 晶格度量识别、内置 Bravais 路径表、非标准基矢与中心化晶格限制 | 已明确不是空间群小群/不可约表示算法，并给出显式 `BravaisType` 例 |
| `showCrystalStructure` / `showBrillouinZone` | 新增 | 直接/倒格子坐标、磁矩来源、箭头、`Axes`/`Boxed`、默认平行投影 | 全部公开选项均有显式调用和实际图形输出 |
| `orbitalTable` | 展示改为 `Grid` | 不把显示层误写为数学数据层改变 | 页面明确底层仍为 `Association`/`List`，可编程读取 |
| `hoppingData` / `transformHoppings` | 新增 | `H(R)[a,b]=<0,a|H|R,b>` 约定、整数晶胞变换及失败边界 | 参数、返回 Association 与所有选项都有实例 |
| `buildBlochHamiltonian` / `buildRealSpaceHamiltonian` / `buildSlabHamiltonian` | 新增 | 周期相位、有限边界、slab/ribbon/rod 方向与稀疏返回 | 每个调用形式和每个选项均有实际结果 |
| `surfaceGreenFunction` / `surfaceSpectralFunction` | 新增 | 主层条件、半无限方向、收敛与展宽失败 | 全部公开选项均有独立可运行例 |
| `wilsonLoop` / `berryPhase` / `berryCurvature` | 新增或行为更新 | 前向链、SVD 幺正化、轨道中心缝合、闭路条件与方向 | 返回格式、所有选项和失败条件均有实例 |
| `plotWilsonLoop` | 新增并重写主例 | Wilson 谱应显示为未连线采样点，而不是暗示连续插值的折线 | Chern 绝缘体与格点 BHZ（Z2）均使用 `ListPlot` 的 `Joined -> False`、`PlotMarkers -> Automatic`；所有公开选项各有实例 |
| `plotSurfaceSpectrum` | 新增并重写主例 | 表面谱不能只用平庸模型，应显出拓扑边界态 | 同一套 QWZ Chern 与格点 BHZ（Z2）实空间 hopping 分别给出手征和螺旋表面谱；所有公开选项各有实例 |
| `pointChernNumber` / `findGaplessPoints` | 新增 | 封闭立方面通量、整数荷、有界网格精化与不保证完备性 | 全部公开选项均有显式例子 |
| `getMSGElemFromMSGCorep` / `getTBBandCorep` | Corep 依赖与显示行为更新 | 只接受 MSGCorep 生成的有序操作，可选依赖失败语义，`Grid` 展示，单值/双值范围 | 复用 `{164,87}` 的 Gamma/K/A 复杂群和双群证据，未用 `{1,1}` 代替 |
| `CrystalAndKPaths` / `RealSpaceAndTopology` / `Corepresentations` | 新增教程 | 将参考页例串成端到端流程 | 输入/输出直接复用已验证证据 |
| `GeneralExamples` / `bandManipulate` | 修正文档例 | MoS2 参数与旧例的 `{9,11,13}` 子群一致；不保存依赖旧内核的动态输出；不重复实现已有 Chern API | MoS2 用 `brokenSymmetryInitRules` 显式闭合子群，Chern 直接调用 `pointChernNumber`，所有表格用 `Grid`，`bandManipulate` 仅保留可重新执行的输入 |

## 最终检查记录

- 原中文手册 17 页：已逐页阅读并检查版式。
- 原英文手册 11 页：已逐页阅读并检查版式。
- 新版功能草稿 13 页：已逐页阅读；只用来补充 2.0 功能，不作为工程术语的语言模板。
- 当前英文与简体中文各 69 页：59 个函数参考页、9 个教程和 1 个 Guide。
- 2026-08-15 增量覆盖 23 个新增/变更函数，新增 18 个参考页和 3 个教程；中英文代码与输出使用同一生成证据。
- 生成后的 86 个 notebook：新增双语参考页与更新后的双语 Guide 已通过 canonicalize、Unicode/期望片段检查和独立 Mathematica 解析；既有页面保持内容不变。
- 2026-08-14 移除普通建模主流程中不参与 Hamiltonian 约束的额外乘法报告后，英文/简体中文生成源已同步；74 个 notebook 重新 canonicalize，并完成 Unicode/期望片段与独立 Wolfram 解析检查。
- 真实排版：中英文 `init`、入门、综合范例和七晶系共 8 份 PDF、173 页已通过 FrontEnd 生成；逐页联系表与重点页高分辨率检查完成。
- 真实渲染曾发现入门教程的四带符号矩阵超出右边界；已只缩小该完整矩阵的显示比例，保留所有矩阵元，重新渲染后左右边界完整。
- 2026-08-16 拓扑绘图增量：`plotWilsonLoop` 的 Chern/BHZ 主例改为未连线散点；`plotSurfaceSpectrum` 使用对应 Chern/Z2 模型；相关双语参考页和教程完成 canonicalize、结构/Unicode、双语输入输出一致、中文漏译与 clean-kernel 重放。
- 2026-08-14 新增六个分圆域精确零空间公共接口的英文与简体中文参考页；中英文可执行单元和保存输出完全一致，六页共 25 组输出已在独立 clean kernel 中重放通过。
- 2026-08-14 完整执行 `sh Developer/Documentation/ValidateDocumentationRelease.sh`：10/10 阶段通过；43 个英文页 fresh-kernel 重放通过，公共 API 与选项覆盖门禁通过。
- 2026-08-16 导航增量：除 `Guides/MagneticTB` 主页本身外，英文和简体中文其余 63 页全部含有指向 `paclet:MagneticTB/guide/MagneticTB` 的可点击链接；链接覆盖已加入静态门禁。
- 2026-08-16 主页分类增量：英文和简体中文 Guide 同步重排为“安装、更新与首个模型”入门区，以及晶体/群论、Hamiltonian、绘图、物性、分圆域五个功能分类；54 个参考页入口和 9 个教程入口全部保留且目标存在。
- 2026-08-16 示例初始化增量：英文和简体中文各 59 个参考页均不再使用只有 `Needs[...]` 的独立初始化单元；两种语言各 319 个 `Input` 都把对应的新/原版程序加载语句与实际示例代码放在同一单元。代表页真实内核重放和中英文 FrontEnd 渲染均通过。
- 2026-08-16 示例精简增量：`init`、`initfromrep` 的选项例删除重复的长模型样板，只保留目标选项和直接可读的结果；`hop`、`readHR` 的文件例直接使用 `$TemporaryDirectory`，不再要求用户管理多组临时文件变量。`showHoppingParameters` 和基于已求解键层的 `hop` 例仍保留完整模型初始化，确保复制到全新内核即可运行。当前程序已明确移除 `init` 的 `SiteLocalData` 选项，帮助页同步改为说明：自动 Induced 表示由 `basisFunctions` 推导，已有精确格点对称矩阵时使用 `initfromrep`。五个受影响页面的英文和简体中文 Input/Output 一致，44 组保存输出已在独立全新内核中重放通过。
